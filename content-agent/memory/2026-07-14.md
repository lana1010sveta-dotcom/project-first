# 2026-07-14

## Сессия 1

Тема: запуск Agent Produsser — отладка и первый старт

### Сделано
- PTB 20.8 не поддерживает Python 3.14 → обновили до PTB 22.8
- Переписан запуск bot.py: `asyncio.run(main())` → `post_init/post_shutdown` хуки (PTB 22.x требует синхронный `run_polling()`)
- Anthropic API недоступен (белорусская карта) → переключили planner.py и generator.py на OpenRouter (OpenAI-совместимый клиент, модель `anthropic/claude-sonnet-4-5`)
- Отладка .env: PowerShell записывал с BOM → перезаписали через Write tool без BOM
- Добавлен `OPENROUTER_API_KEY` в .env (скопирован из CopiRyt, потом обновлён на новый)
- Бот запущен — `/start` ответил, `/plan` сгенерировал 25 тем
- DALL-E 3 не работает (нет баланса OpenAI) → добавлен graceful fallback: если картинка не генерируется, пост уходит на одобрение без неё
- Все изменения закоммичены (коммит 97dafc0)

### Решения
- OpenRouter вместо прямого Anthropic — единственный рабочий вариант для белорусской карты
- Картинки отключены до пополнения OpenAI; бот работает без них

### TODO на потом
- Пополнить OpenAI и протестировать генерацию картинок
- Протестировать полный флоу: /next → одобрение ✅ → публикация в @missisdev
- Перенести бота на сервер (Railway/VPS) для работы 24/7
