# Аудит c:\projects — 14.07.2026

## Общая картина

Три группы активов: (А) проекты, которые уже почти умеют приносить деньги, но у каждого не хватает последнего звена; (Б) «маркетинговая машина» для соцсетей, собранная на 80%, но не запущенная в режиме 24/7; (В) вспомогательное и мусор, который съедает диск (актуально из-за переезда на SSD).

Главный вывод: **ни один проект не доделан до точки приёма денег, при этом почти у всех до этой точки осталось 1–3 шага.** Стратегически выгоднее закрыть последние звенья в 3–4 проектах, чем начинать что-то новое.

| Проект | Готовность | До денег осталось | Тип дохода |
|---|---|---|---|
| salon-backend | ~85% | приём Telegram Stars | 🔁 **подписка (пассивный)** |
| resume | ~90% | ключи ЮKassa/WebPay | 💵 разовые платежи (полупассивный) |
| ckviz-rijeltor | ~90% | боевой деплой + сдача клиенту | 💼 разовый заказ |
| stroyka-nikolay | ~80% | материалы от Николая, сдача | 💼 разовый заказ |
| numerology | ~50% (этап 1 готов) | деплой бота + этап 2 | 💼 заказ, потом возможна подписка |
| content-agent | ~80% | баг публикации + сервер | 📣 растит канал → приводит заказы |
| Портфолио + визитки | ~85% | мелкие битые ссылки | 📣 приём заявок |
| tg-salon-catalog («коробка») | рабочий MVP | скорость фото, дизайн-долг | 💵 продажа копий |

---

## Группа А — проекты у самой кассы

### 1. salon-backend — 💳 единственный настоящий подписочный продукт

**Положительное.** Самый зрелый проект: боевой деплой на Railway + Supabase, полный флоу «регистрация мастера → запись клиента → уведомление» проверен на живой клиентке (Татьяна, «Афродита», PRO-триал). Реализованы напоминания 24ч/2ч, запрос отзывов, win-back спящих клиентов на 30/60/90 дней, защита от двойного бронирования. Тарифы free/PRO (300 Stars/мес) описаны в API, модель `SubscriptionEvent` под платежи есть.

**Доделать (по важности):**
1. **Приём оплаты Telegram Stars — не реализован вообще.** В коде нет ни `sendInvoice`, ни обработчиков `pre_checkout_query`/`successful_payment`; PRO сейчас выдаётся вручную в БД. Это единственное, что отделяет от пассивного дохода.
2. Баг «запись → HTTP 500 на пустом салоне»: на бэкенде уже стоит `try/except` в `app/routers/app_router.py:254`, но фронтовый демо-фоллбэк (фейковые id `s1`…) не починен, и в `CLAUDE.md:108` пункт висит открытым — довести и закрыть.
3. Мульти-мастер: у Татьяны 4 мастера, а система различает только салон (один слот на весь салон). Блокер для реальных денег с её салона. Нужна таблица `masters`.
4. AI-консультант (PRO-фича) заявлен, но не реализован — `anthropic` в requirements мёртвый.
5. Тестов ноль (папка `tests/` пустая).

**Шаги до монетизации:**
1. Реализовать оплату Stars: `createInvoiceLink` на 300 XTR → обработчик `pre_checkout_query` → по `successful_payment` записывать `SubscriptionEvent` и ставить `plan=pro, plan_expires_at=+30 дней`.
2. Добавить фоновую задачу «подписка истекла → перевести на free + напомнить мастеру» (в тот же цикл, что `reminders.py`).
3. Дочинить баг записи на пустом салоне (до подключения новых мастеров).
4. Сделать мульти-мастер — без этого Татьяна не сможет пользоваться полноценно и рекомендовать дальше.
5. Только после этого — привлекать новых мастеров через канал @missisdev и Instagram: контент «как салон в Лунинце перешёл на онлайн-запись» — готовый кейс.

⏰ Триал Татьяны идёт — **если оплату не сделать до его конца, продлевать будет нечем, кроме ручной выдачи.** Самый срочный денежный шаг во всём каталоге.

### 2. resume — деньги написаны, но выключены

**Положительное.** Фронт готов: 3 макета, live-preview, PDF, ATS-разбор. Платёжный бэкенд написан **полностью** — ЮKassa (РФ, 199 ₽) и WebPay/ЕРИП (РБ, 6.9 BYN), вебхуки, привязка доступа к email, Upstash-хранилище. Честная модель без подписочной ловушки. Vercel-проект привязан.

**Доделать:**
1. Получить и вписать ключи: `YOOKASSA_SHOP_ID/SECRET_KEY`, `WEBPAY_STORE_ID/SECRET_KEY`, `UPSTASH_REDIS_REST_URL/TOKEN` — сейчас всё пустое, `startCheckout` возвращает false.
2. Сверить подпись WebPay с боевым личным кабинетом — в `api/_lib/webpay.js:69` прямо написано, что состав полей не проверен.
3. Убрать демо-коды `RESUME-PRO`/`СПАСИБО` из `src/lib/paywall.ts:26` — любой, кто откроет исходники, получит Pro бесплатно. Pro-флаг в localStorage обходится руками — после включения оплаты добавить серверную проверку через `api/access.js`.
4. Косметика: README — всё ещё шаблон Google AI Studio, name пакета `react-example`.

**Шаги до монетизации:** регистрация в ЮKassa (и/или WebPay для РБ) → ключи в Vercel env → webhook-URL в ЛК → тестовый платёж → убрать демо-коды → трафик из портфолио и соцсетей («сделай резюме за 10 минут, 199 ₽»). Ближайший к запуску **полупассивный** продукт: после включения оплаты продаёт себя сам.

### 3. ckviz-rijeltor — готовый квиз, который не доставляет лиды

**Положительное.** Квиз реализован полностью и качественно: ветвление по целям, маска телефонов BY/RU, localStorage, отличная документация (quiz-passport на 32 КБ). Боевая отправка в Telegram написана (Netlify function).

**Доделать — критичное:**
1. **На Vercel сейчас работает заглушка**: `api/submit.js:5` возвращает `{demo:true}` и никуда не шлёт. Если риелтор откроет Vercel-версию — лиды будут пропадать молча. Выбрать один хостинг (Netlify, где боевая функция) и погасить второй.
2. `CONFIG.realtorName = '[ИМЯ РИЕЛТОРА]'` (`index.html:493`) — плейсхолдер в проде.
3. Весь `PLAN.md` — 5 блоков, ни один не отмечен: согласование с риелтором, токен бота, email-дубль через Resend, Метрика, домен, инструкция.

**Шаги до монетизации:** созвон с риелтором → его имя/токен/chat_id → деплой на Netlify с env → тестовый лид до его Telegram → Метрика + домен → сдача и оплата. Проект на 90% готов, деньги лежат в одном созвоне с клиентом.

### 4. stroyka-nikolay — блокирован на стороне заказчика

**Положительное.** Лендинг собран целиком, форма с антиспамом шлёт в Telegram (@Sovfok) с graceful-fallback, ТЗ подробное (`tz-stroyka-nikolay.md`), деплой на Vercel есть.

**Доделать:** галерея «Мои работы» — CSS-плейсхолдеры вместо фото; 13 вопросов из ТЗ клиенту не закрыты (точный телефон, районы, куда слать заявки, домен); Метрики нет; упомянутый в ТЗ `wireframes.html` отсутствует.

**Шаги:** один список вопросов Николаю (фото объектов — блокирующий пункт по ТЗ) → вставить фото и ответы → env-переменные Telegram в Vercel и тестовая заявка → Метрика → сдача. Проект висит с 18.06 — стоит напомнить заказчику.

### 5. numerology — этап 1 готов, но не сдан

**Положительное.** Лид-бот полностью написан и покрыт тестами (имя → дата → интерес → уведомление нумерологу + Google Sheets), сильный research (33 КБ) и спека.

**Доделать:** бот **не задеплоен** — Procfile для Railway не создан, `.env` и credentials не заведены; README устарел («разработка не начата», хотя бот готов); этап 2 (калькулятор «код успеха» + лендинг) не начат; мелочи из progress.md (KeyError на неожиданном callback, очистка user_data).

**Шаги до монетизации:** ключи от клиентки (BOT_TOKEN, таблица) → Procfile + Railway → тестовая заявка → **сдать и закрыть оплату этапа 1** → потом продавать этап 2 (калькулятор). Подписочный потенциал: ежемесячные «прогнозы по коду» для её клиентов — разговор после сдачи этапа 1.

---

## Группа Б — машина для соцсетей (Телеграм + Инстаграм)

### 6. content-agent («Продюссер») — ключ к задаче «развивать Телеграм»

**Положительное.** MVP собран целиком: планировщик тем, генератор постов, бот одобрения @my_prodjuser_bot, автопостинг по расписанию (ежедневно 09:00 Минск, план 1-го числа), 17 проходящих тестов. Лучший актив под цель регулярного контента.

**Доделать:**
1. **Прямой баг:** `publisher.py:18` безусловно открывает файл картинки, а картинки сейчас отключены (`image_path=""`) → публикация в @missisdev упадёт с `FileNotFoundError`. Фоллбэк прикрывает только предпросмотр, не публикацию.
2. Полный флоу `/next → ✅ → пост в @missisdev` не проверен ни разу.
3. Сервера нет — бот работает, только пока включён компьютер. Автопостинг «раз в день в 09:00» без сервера — фикция.
4. Instagram-паблишера нет (заявлен этапом 4).

**Шаги:** починить публикацию без картинки (text-only `send_message`) → прогнать полный цикл в канал → задеплоить на Railway/VPS 24/7 → месяц стабильного постинга в @missisdev → потом Instagram. Пока Instagram-автоматизации нет нигде, реалистичный план — **Инстаграм вести руками**, беря тексты у CopiRyt/content-agent, автоматизацию добавить этапом 4.

### 7. CopiRyt — рабочий личный копирайтер

**Положительное.** Полный мультиагентный конвейер (research → структура → текст → редактор → критик), стилевые правки, контент-план, тренды через Apify, картинки, LinkedIn/Twitter-форматы. Заглушек в коде нет.

**Доделать:** `apify-client` не внесён в requirements.txt — на чистой машине бот не стартует; в логе конфликт двух запущенных инстансов; `agents/image_generator.py` и `agents/trend_scout.py` не закоммичены; Instagram-форматов нет (есть LinkedIn/Twitter, которые менее нужны).

**Шаги:** это не продукт на продажу, а инструмент — довести requirements, закоммитить untracked-агентов, добавить формат «пост Instagram + 5 сторис» вместо LinkedIn. Дальше он кормит контентом обе соцсети.

### 8. jarvis — каркас, ждущий сервера

**Положительное.** Продуманная DNA-структура (SOUL/MEMORY/GOALS), 53 маркетинговых скилла (соцсети, CRO, email — пригодятся для продвижения своих продуктов), бот v2.1.0 с полной инструкцией установки на VPS.

**Доделать:** бот не задеплоен; векторная память написана, но отложена до VPS (нужен pgvector + VOYAGE_API_KEY); скилл reminder ждёт бота.

**Шаги:** деплой бота на VPS — тот же сервер, что и для content-agent (один VPS закроет оба проекта). До этого jarvis полноценно работает как локальный каркас — приоритет ниже, чем у content-agent.

### 9. Портфолио (thoughts/shared) + визитки — витрина, куда придут заказчики

**Положительное.** Портфолио живое (`my-website-bay-psi.vercel.app`), с ценами в BYN/RUB, формой в два канала (Formspree + Telegram/Sheets), юридическими страницами. Визитка vizitka-systems задеплоена с OG-превью.

**Доделать:**
- `vizitka-site/index.html:452` — кнопки WhatsApp и Instagram ведут в `#` (пустышки). Для визитки, задача которой — контакт, это прямая потеря заявок.
- Бриф портфолио: не закрыты реальное фото, реквизиты ИП, OG-картинка 1200×630, отзывы клиентов.
- `landing` (цветочная лавка): **все** CTA и соцкнопки — `href="#"`, формы нет. Как демо в портфолио сойдёт, как рабочий лендинг — нет. Решить: либо экспонат портфолио (так и подписать), либо доделать форму.

**Шаги:** проставить реальные ссылки в vizitka-site (15 минут) → добавить отзывы Татьяны и Николая после сдачи их проектов → карточки-кейсы в портфолио → эти же кейсы = контент для Телеграма/Инстаграма.

---

## Группа В — остальное

- **tg-salon-catalog** — рабочая «коробка». Долги из TODO: сжатие фото при загрузке, коралловый дизайн доведён только на главной, «ближайший слот» считается по UTC вместо TZ салона. Грабли: **Vercel не автодеплоится** — код в git расходится с продом; настроить auto-deploy или всегда помнить про `vercel --prod`. Монетизация — продажа копий под клиента.
- **obraz** — дизайн-песочница, смотрит в боевой бэкенд. Норм как демо для продаж «коробки». Собственных долгов нет.
- **repetitor-music** — семейный, не монетизационный. Блокер: прототип не проверен на телефоне и голосе Лёни (нужен https — как раз cloudflared). Риск №1 поведенческий, а не технический.
- **pitch-parser** — рабочий инструмент, продукта из него нет. **⚠️ Безопасность: в `pitch-parser\.env` лежит живой ключ OpenRouter открытым текстом, рядом — приватный ключ Google service account.** В git они не попали (спасает .gitignore), но ключ OpenRouter стоит перевыпустить, а json — убрать из папки проекта. У папки нет собственного .gitignore — защита держится только на корневом.
- **my-bussines** — две заметки по цветочной доставке, тихий архив. **protection** — презентация-защита, актуальна. **link-projects** — один docx со ссылками.

---

## Что удалить и почему

Безопасно удалить прямо сейчас (мусор, ~60+ МБ и меньше путаницы):

| Что | Почему |
|---|---|
| `salon-backend\venv\` (59 МБ) | пересоздаётся `pip install -r requirements.txt`; на диске C, где нет места |
| `landing\lending.html` | дубль `index.html`, отличается одной строкой с опечаткой — старая версия |
| `vizitka-systems.html` (в корне) | устаревший дубль `vizitka-systems\index.html` с **битой** картинкой; живая версия — в папке |
| `protection\~$ылки на мои проекты.docx` | lock-файл Word (162 байта), исходного документа рядом нет |
| `CopiRyt\deepseek_markdown_20260604_*.md`, `bot.log` | черновик-остаток и лог с трейсбеком |
| все `__pycache__\`, `.pytest_cache\` | байткод, пересоздаётся сам (numerology, pitch-parser, tg-salon-catalog, content-agent) |
| `my-speaking-style\` | пустая с 6 июня. Либо удалить, либо положить туда гайд стиля — он бы усилил CopiRyt и content-agent |
| `.netlify\` (в корне) | конфиг от 19.05 публикует весь `C:\projects` как сайт — опасная бессмыслица, кэш деплоя |

Решить осознанно (не удалять автоматически):

- **`gstack\` — 1,26 ГБ, крупнейший объект каталога.** Набор скиллов, ставился намеренно. При дефиците места на C — первый кандидат на перенос на новый SSD (вместе со всем c:\projects по плану симлинка); удалять не предлагается.
- **`cloudflared.exe` (51 МБ)** — нужен для теста repetitor-music на телефоне. Оставить, но добавить в `.gitignore`: сейчас git считает его кандидатом на коммит.
- **`obraz\refs\` (177 КБ) и дубль `photos\` (2,8 МБ ×2 с tg-app)** — refs можно снести после финализации дизайна; дубль фото оправдан (два независимых деплоя).
- **jarvis: `ui-ux-pro-max` лежит дважды** (в т.ч. CSV на 745 КБ в каждой копии) — копия была осознанной, но одну можно заменить симлинком.
- **resume: зависимости `@google/genai`, `express`** — мёртвые остатки AI Studio, вычистить при следующей работе над проектом.
- В git-статусе корня висят **незакоммиченные удаления** (`.agents/skills/niche-validator/*`, `vizitka.html`, `photo for site_cr.jpg`) — закоммитить, чтобы статус перестал шуметь.

---

## Приоритеты (рекомендуемый порядок)

1. **salon-backend: оплата Stars** — единственный подписочный (пассивный) доход, и триал Татьяны тикает.
2. **resume: ключи ЮKassa/WebPay** — продукт продаёт себя сам, осталась административная работа.
3. **ckviz-rijeltor: созвон с риелтором + Netlify** — самые быстрые «живые» деньги за почти готовую работу.
4. **content-agent: баг публикации + VPS** — запускает регулярный Телеграм, который приводит клиентов на пункты 1–3.
5. stroyka-nikolay и numerology — двигаются письмом заказчикам (материалы/ключи), почти не требуют времени.
6. Инстаграм — пока руками, на текстах из CopiRyt; автоматизация — этап 4 content-agent, не раньше.

Отдельно: **перевыпустить ключ OpenRouter из pitch-parser** и при переезде на SSD первым переносить `gstack` (1,26 ГБ).
